<%= form_with(model: current_user, url: relationship_path(user.id), method: :delete, remote: true) do |f| %>
  <%= hidden_field_tag :following_id, user.id %>
  <%= f.submit 'フォロー中', class: 'btn btn-info' %>
<% end %>