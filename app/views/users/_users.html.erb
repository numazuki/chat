<% if tab_name == "search"%>
  <div class="form-group search_form">
    <%= form_with(url: users_searches_path,local: true, method: :get, class: "") do |f| %>
      <%= f.text_field :keyword,class: "search_input form-control text-center",placeholder: "ユーザー名　ID　プロフィールを入力"  %>
    <% end %>  
  </div>
<%end%>



<div class="contents <%=tab_name%>_contents">
  <% users.each do |user| %>
    <%unless current_user == user%>
      <%= link_to(user_path(user), class: 'user_cell') do %>
        <div class="user_content d-flex flex-row">
          
          <div class="user_image">
            <img src="<%= user.avatar %>" class = "icon_image user">       
          </div>
          <div class="user_prof">
            <h5 class="text-dark"><%=user.name%></h5>
            <p class="text-secondary">@<%=user.search_id%></p>
            <p class="demigrey"><%=user.profile%></p>
          </div>
          
        </div>
      <%end%>
    <%end%>
  <% end %>
  
</div>

<script>
  var search_input = document.getElementById("keyword")
  search_input.onkeypress = submit_cancel;
  
  function submit_cancel(){
    console.log('aaa')
      if( window.event.keyCode == 13 ){
          return false;
      }
  }

</script>
